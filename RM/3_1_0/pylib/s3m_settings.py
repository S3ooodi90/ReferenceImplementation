# Global, user editable settings for the S3Model Python library

import os

# Export location for Data Model schemas.
DM_LIB = os.path.join(os.path.curdir, "dmlib")

# Access Control System (simulation)
ACS = ['Public', 'Private', 'Secret', 'PII']
